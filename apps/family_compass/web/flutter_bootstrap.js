{{flutter_js}}
{{flutter_build_config}}

// This is a local prototype, so always load the current build. A production
// release can add an explicit offline-update policy before enabling a service
// worker again.
_flutter.loader.load();
